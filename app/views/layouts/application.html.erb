<!DOCTYPE html>
<html>
  <head>
    <title>Prueba Siscotel</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-100">
    <%# <div class="max-w-md mx-auto mt-4 px-4"> %>
      <div class="fixed bottom-5 right-5 z-[9999] flex flex-col gap-4 w-full max-w-sm">
    <%# Notificación de Alerta (Rojo) %>
    <% if flash[:alert] %>
      <div id="alert-box" class=" overflow-visible flex items-center p-4 text-red-800 rounded-lg bg-red-50 border border-red-200 shadow-xl animate-fade-in z-999" role="alert">
        <svg class="flex-shrink-0 w-4 h-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
          <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5ZM9.5 4a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3ZM12 15H8a1 1 0 0 1 0-2h1v-3H8a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1v4h1a1 1 0 0 1 0 2Z"/>
        </svg>
        <div class="ml-3 text-sm font-medium">
          <%= flash[:alert] %>
        </div>
        <button type="button" onclick="this.parentElement.remove()" class=" overflow-visible ml-auto -mx-1.5 -my-1.5 bg-red-50 text-red-500 rounded-lg focus:ring-2 focus:ring-red-400 p-1.5 hover:bg-red-200 inline-flex items-center justify-center h-8 w-8 z-999">
          <span class="sr-only">Cerrar</span>
          <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
          </svg>
        </button>
      </div>
    <% end %>

    <%# Notificación de Éxito (Verde) %>
     <% if flash[:notice] %>
      <div id="notice-box" class="flex items-center p-4 text-green-800 rounded-lg bg-green-50 border border-green-200 shadow-xl animate-fade-in" role="alert">
        <svg class="flex-shrink-0 w-4 h-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
          <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5Zm3.707 8.207-4 4a1 1 0 0 1-1.414 0l-2-2a1 1 0 0 1 1.414-1.414L9 10.586l3.293-3.293a1 1 0 0 1 1.414 1.414Z"/>
        </svg>
        <div class="ml-3 text-sm font-medium">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>
  </div>
    </div>
    <%= yield %>
    <script>
    setTimeout(() => {
      const boxes = document.querySelectorAll('[role="alert"]');
      boxes.forEach(box => {
        box.style.opacity = '0';
        box.style.transition = 'opacity 0.5s ease';
        setTimeout(() => box.remove(), 500);
      });
    }, 4000);
  </script>
  </body>
</html>